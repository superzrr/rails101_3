<div class="col-md-12">
  <div class="group">
    <% if current_user && current_user==@post.user %>
    <%= link_to("Edit", edit_group_post_path(@group,@post), class: "btn btn-primary pull-right")%>
    <% end %>
  </div>

  <p><%= render_post_ceonten(@post) %></p>

</div>
